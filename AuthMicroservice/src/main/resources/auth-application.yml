server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: batuhan
    password:
    url: jdbc:postgresql://localhost:5432/AuthDB
  jpa:
    hibernate:
      ddl-auto: update